# Optional: enables analytics when a valid GA4 measurement ID is supplied.
VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Optional: enables approved-review loading and moderated review submissions.
# Base64-encode the Firebase web config JSON as one line before assigning it.
VITE_FIREBASE_CONFIG_BASE64=
